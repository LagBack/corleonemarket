# Servidor
PORT=3000
NODE_ENV=development

# MySQL (usuários, sessões, perfis)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=corleone_market

# Opcional: apaga db.json (ações/mercado) na inicialização
# RESET_DB=true
